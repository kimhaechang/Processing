```
void setup(){
  size(800,300);
  textSize(150);
}
int i, dir=1, sp;
void draw(){
  background(0,0,255);
  fill(0,125,125);
  text("Graphics", i, 150);
  if(i>width) dir=-1;
  if(i<0) dir=1;
  i=i+dir*sp;
}
void keyPressed(){
  sp=key-'0';
}
```
